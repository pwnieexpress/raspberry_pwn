RaspberryPwn initial setup and Metasploit initialization:

- Soft-float Debian “wheezy” img
- dhcp is auto and ssh enabled
- user: pi password: raspberry

passwd

sudo raspi-config

- expand fs
- memory split
- moderate overclock
- reboot

git clone https://github.com/pwnieexpress/Raspberry-Pwn.git

cd Rasperry-Pwn

sudo ./INSTALL_raspberry_pwn.sh

- reboot

cd /opt/metasploit/msf3

- avoiding dependency hellz

sudo apt-get install libpq-dev

sudo gem install pg

- avoiding moar dependency hellz

sudo apt-get install ruby1.8-dev ruby1.8 ri1.8 rdoc1.8 irb1.8

sudo apt-get install libreadline-ruby1.8 libruby1.8 libopenssl-ruby

sudo apt-get install libxslt-dev libxml2-dev

sudo gem install nokogiri

sudo bundle install

sudo msfconsole



